query ParkingRates($where: pgdb_parking_ratesWhereInput!) {
	pgdb_findManyparking_rates(where: $where) {
		id 		
		created_at 
		min_hours 
		min_amount 
		variable_amount
		is_active 		
		vehicle_types {
			id
			code
			description
		}
	}
}
