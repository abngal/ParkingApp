query ParkingTransactionsAndRelated($where: pgdb_parking_transactionsWhereInput!) {
	pgdb_findManyparking_transactions(where: $where) {
		id
		created_at
		vehicle_plate
		datetime_in
		datetime_out
		amount
		parking_rates {
			min_hours
			min_amount
			variable_amount
		}
	}
}